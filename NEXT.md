# 接下来做什么

按优先级排。每项写清楚**为什么做**、**怎么做**、**做完怎么算完成** —— 没有完成判据的
条目不该开工。

现状见 [README](README.md)。一句话：**一段话 → 补用例 → 写代码 → 验 → 按名字取**，
真实模型跑通了（Haiku 5/5 一次写对），2451 行，98 个单测 + 6 个语料。

---

## 0. 补出来的用例到底靠不靠谱

整套设计现在压在一个假设上：**模型补的用例，期望值是对的。** 一条算错的用例会把
正确的实现判死，而且极难排查 —— 它比漏个 bug 难查得多。

目前的证据只有一轮：`examples/rank.json` 补出 8 条，人工复核 8/8 对。
**一个需求、一个模型、一轮。** 这撑不起那个假设。

怎么做：

- 拿 5 个需求各补一轮，**逐条人工复核期望值**，记错误率
- 错误率不为零的话（大概率不为零），要答的问题是：错在哪类点上？
  是边界（空、单元素）、是歧义（并列、舍入）、还是纯算错？
- 按错误类型决定下一步：某类总错就在 `propose.py` 的 prompt 里点名警告；
  普遍不可靠就得让调用方先过目再用（`--review-tests`）

**完成判据**：一个带数字的结论 —— "N 个需求 × M 条用例，期望值错 K 条，
错的都集中在 X 类点上"。以及据此决定生成用例要不要默认阻断。

---

## 1. 修复循环还没被压力测试过

`examples/rank.json` 用 Haiku 跑 5 次，5 次都是**一次就写对**。三次重试那条路一次
也没走到，"结构化反馈到底有没有用"仍然没有答案。

原来的对照组实验（`max_attempts=1` 跑 5 次和 3 次比成功率）做不出区别：两边都 5/5。
**得先找到一个 Haiku 做不对的需求**，否则修复循环就是一段没人验过的代码。

- 往 `examples/` 加 2~3 个更难的需求。难在**需求本身容易误读**（边界怎么算、
  并列怎么处理、空值算 0 还是跳过），不是难在算法复杂 —— 后者模型一样会做，
  只是慢；前者才会真的写错
- 每个需求跑 5 次 × {max_attempts=1, max_attempts=3}，比成功率
- 顺手记：每次卡在哪道关卡

**完成判据**：拿到一个 `max_attempts=1` 明显做不过 `max_attempts=3` 的需求，
并记下差多少。找不到的话，"修复循环在这个难度区间没用"也是结论，要如实写进
README，而不是继续留着一段没验过的代码。

---

## 2. 场景太窄

整个项目从头到尾只用过 **4 个不同的任务**（分组求和、分档收费、统计行数、排名次），
而且都是小的纯数据变换。单测数量看着不少，但它们全都挤在这 4 个任务上。

最划算的动作是**加需求**，不是加测试 —— 往 `examples/` 塞 5 个形态不同的
（文本解析、日期处理、嵌套结构重组、多字段排序、带状态的聚合），一次同时喂给
第 0 项和第 1 项。

**完成判据**：10 个以上真实需求跑过一遍，记下成功率和失败都卡在哪。

---

## 3. 需要标定的数字

剩下的不多了，砍完之后只有 4 个：

| 参数 | 现值 | 问题 |
| --- | --- | --- |
| `gen_tests` | 8 | 补几条。多了费 token 也更容易出错用例，少了判据不够厚 |
| `max_attempts` | 3 | 见第 1 项。现在 5/5 都是一次过，这个数根本没被用上 |
| `lookup.MIN_SIMILARITY` | 0.20 | 真改写量到 0.29~0.41，不相干的 0.03，中间很空 —— 但只测了两个需求 |
| `llm.CLI_OVERHEAD_TOKENS` | 22200 | `claude -p` 每次带的固定开销，量的是一个空任务。会随 CLI 版本变 |

---

## 4. 明确**不做**的

- **把砍掉的关卡加回来** —— 除非有真实失败要求它。保留集是唯一一个我会主动
  加回来的，条件是第 0 项发现模型对着可见用例写死答案
- **容器 / microVM 沙箱** —— 现在的子进程沙箱是**正确性沙箱不是安全沙箱**，
  在跑自己的代码这个前提下够用。接外部不可信来源之前必须升级
- **能力注入（http / fs / tools）** —— 纯函数覆盖面比直觉大，先把纯函数走通
- **向量检索** —— 现在全库线性扫，registry 大到扫不动再说。它是**性能**优化，
  不是正确性机制，换的时候别把复验一起"优化"掉

---

## 开工前先读

1. `src/agentjit/propose.py` 的开头 —— 模型自己写测试验自己写的代码是循环论证，
   三条缓解各解决了哪一部分，以及哪一部分解决不了
2. `src/agentjit/verify.py` 的开头 —— 砍掉了哪五道关卡，为什么，以及最可能要
   加回来的是哪个
3. `src/agentjit/lookup.py` 的开头 —— 为什么检索可以很土，以及它土在哪里不要紧
