# 接下来做什么

按优先级排。每项写清楚**为什么做**、**怎么做**、**做完怎么算完成** —— 没有完成判据的
条目不该开工。

现状见 [README](README.md)：验证管线、合成循环、registry、三级查找、call 都已完成
并测过（96 个单测 + 11 个语料用例），但**还没有一次真实模型合成的记录** ——
所以管道通了，数是空的。

---

## 0. 阻塞中：跑通一次真实合成

**这是唯一的阻塞项，做完之前不要开新东西。**

本机 OAuth 凭据过期（上次 `ant auth login` 在浏览器授权那步超时了）：

```bash
ant auth login                              # 完成浏览器授权，别让它超时
.venv/bin/agentjit compile examples/rank.json
```

`examples/rank.json`（并列名次跳号 + 同分按字典序）刻意选了一个没在测试里出现过的
任务 —— 测试里的实现是人写好的，证明不了合成质量。

**为什么它排第一**：整个 M0 的价值主张是"合成 + 验证比 agent 自己做便宜"。
在跑通之前，下面这些数全是未知：Haiku 几次能修对、一次合成花多少 token、
结构化反馈到底有没有用（对比"直接重试三次"）、验证关卡会不会在真实生成的代码上
大量误报。**误报是最该盯的**：语料里的错误实现是人写的，真实模型写出来的代码
长得不一样，关卡可能在正确代码上卡住。

**完成判据**
- `agentjit compile examples/rank.json` 产出 `VERIFIED` 的函数
- 记下：尝试次数、每次卡在哪道关卡、token 消耗
- 同一个需求跑 5 次，记成功率。一次成功说明不了什么

**顺手要量的对照组**：把 `max_attempts` 设成 1 跑 5 次，和 3 次修复的成功率对比。
如果两者差不多，说明结构化反馈没起作用，`prompts.py` 的反馈渲染要重做。

**跑通之后立刻补的一件事**：拿那次真实的 token 数跑一遍

```bash
agentjit bench <handle> -n 200
```

管道已经通了（见文末「已经做完的」），缺的就是一个不是编出来的合成成本。把那个回本点记进
README —— 现在那里写的"第 6 次回本"是从成本模型推的，不是量出来的。

---

## 1. M1 最重要的一件事：判据的第二条入口

整个设计押在"调用方愿意给例子"上。这是**唯一的对冲**，做不出来的话
`EPHEMERAL` 会变成主路径，复用就没了。

按 [correctness.md §5](docs/correctness.md#5-t3--差分测试它其实是歧义定位器)：
独立合成 N 份实现，在生成输入上比对，**分歧点精确指出需求里没说清的地方**，
把它变成一道单选题让调用方裁决。不要让调用方出题，让调用方裁决。

**开工前必须先解决的问题**：Opus 5 移除了 `temperature`，传了直接 400
（见 `llm.py` 的能力表）。制造独立实现只能靠 **prompt 变体**，而变体之间的相关性
比温度变体高 —— 三份实现可能一起犯同一个错，分歧点就找不出来。

先做个便宜的实验再动手：
- 拿同一个需求，用 3 种措辞的 prompt 各合成一份，看在生成输入上分歧率有多高
- 分歧率太低（比如 < 5%）说明这条路在 Opus 5 上不成立，得换方案
  （换模型混合？换 Haiku 保留 temperature？）

**完成判据**：一个**不给任何例子**的需求，经过裁决后能产出 `VERIFIED` 函数。

---

## 2. M1：变形性质（T2）

[correctness.md §4](docs/correctness.md#4-t2--变形性质一条性质抵一万个用例)。
调用方给 3 个例子只验证 3 个点；确认 3 条性质验证的是整个输入空间。

- 从需求匹配性质模板（置换不变 / 守恒 / 幂等 / 空元 / 值域封闭 ...）
- 模型提议 → 调用方确认 → 才计入 `CONFIRMED` 门槛
- **未确认的性质只做警告，绝不阻断**。模型提的性质本身可能是错的，
  拿一条错的性质否决正确代码，比漏个 bug 难查得多

`assertions.py` 已经有了这条路的下半截：3 条模板从例子里挖、运行时核对、违反只
警告并把输入存进测试集。缺的是上半截 —— **确认流**，以及确认之后让性质真的能
阻断（那时函数才够得上 `CONFIRMED`）。先看 `assertions.py` 再动手，别重写。

顺带要答的一个问题：现在的性质是从**例子**挖的，模板只有 3 条，覆盖面窄。
是继续加模板，还是改成"模型提议 + 调用方确认"？后者是文档里写的路，但模型提的
性质更可能是错的，而错的性质代价更高。

**完成判据**：确认的性质能在 200 个生成输入上跑并给出最小化反例。

---

## 3. 需要标定的数字（是调参，不是写功能）

这些现在都是拍的，等有真实数据再回来定：

| 参数 | 现值 | 问题 |
| --- | --- | --- |
| `min_mutation_score` | 0.80 | 语料里 95% vs 72% 区分度够，但没在真实数据上标定过；等价变异体还识别不了，白白算进分母 |
| `holdout_ratio` | 0.3 | 例子本来就少，留 30% 会不会削弱合成质量 |
| `max_attempts` | 3 | 见第 0 项的对照组实验 |
| `fuzz_n` | 200 | 够不够碰到边界 |
| 循环内 `fuzz_n` | 50 | 循环内降采样会不会漏掉该早点反馈的崩溃 |
| `econ.reasoning_tokens` | 600 | **这批里最软的一个数。** 它直接决定回本点，却没有任何实证支撑。要从真实轨迹里量：同一件事让 agent 自己做一遍，thinking + output 实际花了多少 |
| `econ.out_weight` | 5.0 | output/input 的单价比。各家各代在 4~5 之间，但真要对齐得按实际用的模型定价算 |
| `runtime.QUARANTINE_AFTER` | 3 | 连续 3 次就隔离，会不会太急 —— 一个被脏数据连打三次的好函数就没了 |
| `runtime.BATCH` | 50 | 批次越大越省进程启动，出事时重放的代价也越大 |
| `registry.MAX_PROBES_PER_KIND` | 200 | 探针上限。太小会把真事故挤掉，太大测试集变垃圾堆 |
| `lookup.MIN_SIMILARITY` | 0.20 | 真改写量到 0.29~0.41，不相干的 0.03，中间很空 —— 但只测了两个需求，样本太少 |
| `lookup.MAX_CANDIDATES` | 5 | 每个候选一次沙箱复验（~50ms）。命中率 vs 查找延迟 |

---

## 4. 明确**不做**的

- **Tier-1（缓存决策序列但不生成代码）** —— 收益区间可能很窄。要么能编译成代码，
  要么老实走 LLM。等数据说话
- **[tracing 前端](docs/tracing-frontend.md)** —— M4。但它的优先级比设计文档里
  写的要高，因为轨迹天然自带 examples，正好补上第 1 项那个"调用方肯不肯给判据"
  的风险。M1 做完重新评估
- **容器 / microVM 沙箱** —— M2。现在的子进程沙箱是**正确性沙箱不是安全沙箱**，
  在跑自己的代码这个前提下够用。接外部不可信来源之前必须升级
- **能力注入（http / fs / tools / llm facade）** —— M3。纯函数覆盖面比直觉大，
  先把纯函数这条路走通

---

## 已经做完的（留着是因为里面有踩过的坑）

### M0 收尾：compile 和 call 闭环

`registry.py` / `runtime.py` / `econ.py` / `assertions.py`。
`agentjit list / inspect / call / bench / stats / adopt`。

**和原计划不一样的三处**，都在代码顶部的 docstring 里写了理由：

- **`tests.json` 放在 spec 一层，不在版本一层。** 同一个 spec 的所有版本共用一份
  单调增长的测试集，[correctness.md §10](docs/correctness.md#10-测试集是资产代码是可再生的)
  那条"模型升级 = 免费的全库重生成"才有依托。放版本里意味着每次重生成都复制一份，
  之后各自增长，再也合不回去。
- **入参 guard 失败不推进隔离计数。** 没过 schema 的输入根本没进函数，不构成对
  实现的指控；而 `param_schema` 是从两三个例子推出来的，天生偏窄。能算到实现头上
  的只有：运行时崩溃、超预算、返回值不合 schema。
- **后置断言只警告不阻断**，按 [correctness.md §4.2](docs/correctness.md#42-性质从哪来)
  而不是 design.md §8.2 的字面。

**没做完的那半**：`net_savings` 的管道通了，但喂进去的合成成本要么是 0，要么是
回放脚本编的。**这个数现在转正不说明任何问题** —— 它等第 0 项。

---

### M1：规格归一化 + 三级查找

`lookup.py` + `jit.py`，两条完成判据都有测试钉着（`tests/test_lookup.py`）。

动手前量的那个数值得记住：**把"求和"换成"求平均"，字面相似度 0.54，比三种真正的
改写（0.41 / 0.32 / 0.29）都高。** 所以检索必然把错的排在最前面，而这不是换成
向量能解决的 —— design.md §6.1 说向量空间里也一样近。结论是
**检索质量只影响命中率，不影响正确性**，判定全靠复验。

还没做的那一半：

- **L2 现在是全库线性扫 + 字符二元组。** registry 小的时候够用，大了要换成向量
  近邻。但它只是**性能**优化，不是正确性机制 —— 换的时候别把复验一起"优化"掉。
- **`CanonicalSpec` 里的 `intent` 没有真的归一化。** 现在的 `normalize()` 只做
  NFKC + 小写 + 去标点，"求和"和"求总额"仍然是两个串。真归一化要模型（把需求
  压成一句标准话），那会让每次 compile 都多一次 LLM 调用 —— 得先量清楚它能把
  命中率提多少，才知道值不值。
- **`capabilities` / `effect_class` 还没进 hash。** M3 做能力注入之前它们恒为空，
  进不进都一样；但那天到了必须补，否则一个"读文件的求和"会命中"纯函数的求和"。

---

## 开工前先读

1. [design.md §4 接口设计](docs/design.md#4-接口设计) —— 四个操作，其余都是实现细节
2. [correctness.md §2 Oracle 分层](docs/correctness.md#2-oracle-分层) —— 五层判据，
   哪三层不需要标准答案
3. `src/agentjit/verify.py` 里 `verify()` 的 docstring —— 覆盖率为什么是
   "保留集对循环不可见"的例外。这个坑踩过一次
4. `src/agentjit/lookup.py` 的开头 —— 为什么检索可以很土，以及它土在哪里不要紧
