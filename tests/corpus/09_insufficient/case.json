{
  "name": "09_insufficient",
  "why": "no criteria: the code is fine, but with no test cases there is nothing to judge — not cached",
  "impl": "impl.py",
  "spec": {
    "intent": "group rows by type and sum amount; amount may carry a currency symbol and thousands separators, blank counts as 0",
    "param_schema": {
      "type": "object",
      "properties": {
        "rows": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "type": {
                "type": "string"
              },
              "amount": {
                "type": "string"
              }
            },
            "required": [
              "type",
              "amount"
            ]
          }
        }
      },
      "required": [
        "rows"
      ]
    },
    "return_schema": {
      "type": "object",
      "additionalProperties": {
        "type": "number"
      }
    }
  },
  "examples": [],
  "expect": {
    "level": "EPHEMERAL",
    "failing_gates": []
  }
}
